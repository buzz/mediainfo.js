// import { spawnSync } from 'child_process'
// import { resolve } from 'path'

export default () => {
  // TODO: download fixtures
  // process.stdout.write('\nDownloading fixtures...\n')
  // const downloadFixturesScript = resolve(__dirname, 'download-fixtures.sh')
  // const proc = spawnSync(downloadFixturesScript, [], {
  //   cwd: __dirname,
  //   timeout: 10 * 60 * 1000,
  // })
  // console.log(proc.stdout.toString())
  // console.log(proc.stderr.toString())
  // if (proc.status !== 0) {
  //   throw new Error('Downloading fixtures failed!')
  // }
}
